# IDD-0007.spec-git-workflows

## Intent

Приложение предоставляет понятные desktop workflows для stash, merge и rebase,
делегируя изменение репозитория Git и сохраняя его semantics во всех исходах.

## Related Specifications

- IDD-0003
- IDD-0005
- IDD-0006
- IDD-0008
- IDD-0009

## Behavior

- Создание stash доступно из contextual action `Create stash…` корневой группы
  `Stashes` в Repository Tree и из Working Tree / Commit pane. Оба entry point
  запускают один и тот же stash workflow и одну mutation semantics.
- Создание stash является repository-level действием над текущими Working Tree /
  Index. Наличие selected file/folder не меняет scope операции и не превращает
  обычный stash в partial stash.
- Создание stash делегируется Git; после mutation приложение перечитывает
  фактическое состояние repository и не синтезирует stash в Presentation.
- Apply и Pop остаются actions конкретного stash entry.
- Пользователь может merge выбранную branch в current branch.
- Merge сохраняет применимые исходы Git: fast-forward, merge commit, отказ Git и
  conflicts. Приложение не эмулирует merge самостоятельно.
- После успешного merge обновляются history, graph, refs, working tree и index.
- Приложение поддерживает rebase, включая interactive rebase с действиями pick,
  reword, squash, fixup, drop и reorder.
- Interactive Rebase имеет два entry point: manual `Build plan` в Git operations
  и `Interactive rebase from here…` в context menu History. Оба используют один
  plan editor, один `IRepositoryWorkflowService` и один rebase engine.
- History entry point трактует выбранный полный commit SHA как первый commit
  плана; base/upstream вычисляется Git/Application layer как его единственный
  parent. Shortcut доступен только при attached local HEAD, но окончательная
  ancestry/topology/state validation выполняется после клика.
- Текущая Interactive Rebase модель поддерживает только linear ranges. Если
  `base/upstream..HEAD` содержит merge commit, plan явно отклоняется как для
  manual Build plan, так и для History entry point; merge ниже range не мешает.
- UI interactive rebase может быть проще Git Extensions, но сохраняет Git
  semantics.
- Merge или rebase, остановившиеся из-за conflicts, переходят в общий conflict
  workflow и оставляют репозиторий в реальном состоянии незавершенной операции.

## Durable Architecture And Constraints

- Mutating workflows выполняются самим Git или совместимым с Git механизмом, а не
  собственной эмуляцией приложения.
- Каждый завершившийся или остановившийся workflow приводит к перечитыванию
  фактического состояния репозитория.
- Подготовленный Interactive Rebase plan хранит source snapshot: полный HEAD SHA
  и canonical symbolic HEAD ref (или detached identity для generic workflow).
  Перед реальным `git rebase --interactive` service повторно проверяет operation,
  HEAD/ref identity, существование base, актуальный commit set и linear topology.
  Изменение base/upstream в UI инвалидирует подготовленный plan.
- Оба entry point создания stash используют общий presentation/mutation workflow;
  отдельная stash mutation или отдельный refresh lifecycle не вводятся.
- Расширение stash-возможностей не должно менять базовую Git-модель entries,
  Apply и Pop.

## Non-Goals

- Копирование UI Git Extensions для interactive rebase.
- Partial/file-scoped stash и специальные stash modes в первой версии.
- Cherry-pick, Revert и общий Reset workflow в обязательном MVP.

## Acceptance Criteria

- Пользователь может создать stash как из корневой группы `Stashes`, так и из
  Working Tree / Commit pane; оба entry point используют один workflow.
- Обычный Create stash остается repository-level действием и не зависит от
  selected staged/unstaged files или virtual folders.
- Пользователь просматривает stash entries и выполняет Apply и Pop как действия
  конкретного entry.
- Пользователь запускает merge выбранной branch и получает корректный Git outcome.
- Пользователь настраивает и запускает interactive rebase с основными действиями.
- Conflict переводит merge/rebase в единый восстановимый conflict workflow.
- После успешных операций UI соответствует фактическому репозиторию.

## Verification

- UI contract tests проверяют оба Create stash entry point, общий workflow,
  availability через существующий stash state и отсутствие обычного Create stash
  в file/folder context menus.
- На реальных тестовых репозиториях проверяются stash Create/List/Apply/Pop,
  fast-forward merge, merge commit, отказ и conflict.
- Проверяются manual и History entry points interactive rebase, linear-only
  topology, ancestry/root/detached failures, stale HEAD/ref snapshot, каждое
  поддерживаемое plan action и остановка на conflict.
- Для каждого workflow проверяются success и применимые failure scenarios,
  обновление UI и сохранение Git semantics.
